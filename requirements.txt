# ══════════════════════════════════════════════════════════════════════════════
# Root requirements — installs dependencies for ALL modules.
# For selective installation, use per-module requirements instead:
#   pip install -r backend/requirements.txt
#   pip install -r data-processing/requirements.txt
#   pip install -r frontend/requirements.txt
#   pip install -r azure-ai-provider/embedding_service/requirements.txt
# ══════════════════════════════════════════════════════════════════════════════

# ── Backend (FastAPI retrieval API) ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.0.0
langgraph>=1.0.7
langchain-core>=1.2.24
qdrant-client>=1.9.0
httpx>=0.27.0
google-genai>=1.70.0
openai>=1.0.0
langdetect
supabase>=2.0.0

# ── Data Processing (offline pipeline) ──────────────────────────────────────
openpyxl>=3.1
pandas>=2.2
PyYAML>=6.0
tqdm>=4.66
yt-dlp>=2025.1.26
ffmpeg-python>=0.2.0
numpy>=1.26
opencv-python-headless>=4.10
pillow==11.3.0
matplotlib>=3.9
scikit-learn>=1.5
accelerate==1.13.0
einops>=0.8.2
faiss-cpu>=1.8.0
timm>=1.0.0
transformers==4.51.2
ultralytics==8.4.21

# ── Frontend (Streamlit UI) ─────────────────────────────────────────────────
streamlit==1.55.0
